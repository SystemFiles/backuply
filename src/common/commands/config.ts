// Configuration commands to change app defaults

// Command functions 

// export function changeDataLocation(newLocation: string): [ string, Error ] {
//   const conf = AppConfig.getInstance()
//   return conf.changeDatabaseLocation(newLocation)
// }